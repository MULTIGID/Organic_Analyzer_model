project:
  name: "PlantVillage ResNet-50 Leaf Disease Analyzer"
  seed: 42

data:
  root: "D:/NK/University_2/Диплом/Датасети/PlantVillage-Dataset"
  image_size: 224

model:
  pretrained: true
  num_classes: 38

training:
  epochs: 10
  batch_size: 16
  learning_rate: 0.0001
  weight_decay: 0.0001
  num_workers: 0
  patience: 3
  use_amp: true

paths:
  checkpoint: "../../checkpoints/plantvillage/resnet50_plantvillage_best.pt"
  last_checkpoint: "../../checkpoints/plantvillage/resnet50_plantvillage_last.pt"
  results_dir: "../../results/plantvillage"
